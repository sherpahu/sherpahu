#include <stdio.h>
void erjin(unsigned long n);
int main()
{
    unsigned long num;
    while (scanf("%lu",&num)==1)//表示只要输入成功就进入循环
    {
        erjin(num);
    }
    return 0;
}
void erjin(unsigned long n)
{
    int r;
    r=n%2;
    if(n>=2) erjin(n/2);
    putchar(r==0?'0':'1');
    
}
