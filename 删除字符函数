void delChar(char str[],char c)
{
    int i=0,j;
    while(str[i])
    {
        while(str[i]!=c&&str[i])
          i++;
        if(s[i]!='\0')
        {
          j=i+1;
          while(s[j])
          {
              s[j-1]=s[j];
              j++;
          }
          s[j-1]='\0';
        }
    }
}
